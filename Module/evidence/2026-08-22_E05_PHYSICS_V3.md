# E05-PHY-v3：真实指腹、拇指接触与长程二维复杂曲面证据

## 正式状态

- 日期：`2026-08-22`
- 环境：`handcomp`
- 引擎：`MuJoCo 3.6.0`
- 方法：`explicit fingertip reference + FingertipMCC + pad-center/orientation IK`
- 协议 SHA-256：`1db9b32338828cf9afaa736ad39dfeb6aa49d493502d28156af3d870e4a7eb28`
- 评测执行状态：`COMPLETED`
- 基础 5A/5B/5C thresholds：`MET`
- long 2D extreme-surface thresholds：`NOT_MET`
- 综合性能判定：`EXTREME_THRESHOLDS_NOT_MET`
- Finger DP：未导入、未运行、未比较

正式命令：

```bash
/home/ferry/data/Anaconda/envs/handcomp/bin/python -m Module.e05_physics.demo
```

命令在写完 JSON/CSV/NPZ、五段 MP4、screenshots、dashboards 和 HTML 后返回 `0`。
性能阈值是否满足只记录在 summary，不改变评测完成状态。正式 trace 生成后展示层包含
突变后
`118.90 N` 峰值纳入 dashboard 纵轴，并修正 contact-recovered/force-settled 关键帧；
数值 trace、summary 和协议 hash 均未改变。

## 真实 fingertip-body 指腹

唯一标准使用 `24 x 16 x 4 mm` 椭球，并直接注册到：

```text
fingertip, fingertip_2, fingertip_3, thumb_fingertip
```

pad 长轴沿 body `+Y` 近端方向，outward normal 沿 body `-X` 腹侧。长指 pad center local
Y=`-24 mm`，thumb=`-30 mm`。设计 distal-edge 到 rounded-head 间隔最小 `13.5 mm`；
四个标称 pad-down alignment 分别约 `0.999954, 0.999954, 0.999954, 1.000000`。

原 FSR/hand/rounded-tip meshes 全部 `contype=conaffinity=0`。只有四个 pads 与 object geom
具有 collision bits，故任何 `mj_contactForce` 都不可能来自 rounded head。

正式基础平移实际 contact-to-head 最小间隔 `18.995 mm`；长程复杂曲面突变前最小间隔
`14.046 mm`，均高于冻结 `10 mm` 门槛。

## 拇指真实关节姿态

拇指固定标称角为：

```text
q[12:16] = [0.53481, 1.57006, 0.10087, -0.63505] rad
```

该姿态由 `thumb_fingertip` body kinematics 使其 physical belly normal 朝下，不是单独旋转
collision geom。正式 thumb physical-contact probabilities：

| 场景 | thumb contact |
| --- | ---: |
| plane translation | `100.00%` |
| plane rotation | `97.69%` |
| large sphere | `100.00%` |
| long 2D continuous segment | `97.88%` |

`pad_geometry_audit.png` 已人工检查：A 为 object-hidden underside，B 显示三根长指 pad 位于
rounded head 近端，C 清楚显示拇指由关节姿态转向平面且红色 pad 位于其腹侧。彩色短杆
显示 pad outward axes。

## 长程二维复杂连续环境

height-field/oracle 为 `0.60 x 0.84 m`、`301 x 421`，同时依赖 `x,y`。它包含长波、
斜向短波、交叉波纹、四个不同尺度的凸/凹 Gaussian feature 和斜向窄 tanh ridge。

| profile 指标 | 正式值 |
| --- | ---: |
| height range | `22.298 mm` |
| maximum gradient norm | `0.7748` |
| maximum principal curvature | `62.629 1/m` |
| minimum curvature radius | `15.967 mm` |

非网格的 `4 x 6 = 24` vertical-ray audit：maximum analytic/collision height error
`0.01313 mm`，RMS `0.00477 mm`，通过 `0.05 mm` 门槛。

episode 为 `15.0 s`，Y 净位移 `480 mm`，X 方向叠加两种频率形成 S-scan；标称全程
path 约 `567.8 mm`。突变前连续段为 `9.25 s / 406.758 mm`。

## 基础 sanity 结果

| 场景 | continuity | avg contacts | force RMSE | max force | thumb | head clearance | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| plane translation | `1.000` | `4.000` | `0.070 N` | `1.960 N` | `1.000` | `18.995 mm` | MET |
| plane rotation | `1.000` | `3.972` | `0.209 N` | `2.343 N` | `0.9769` | `18.148 mm` | MET |
| large sphere | `1.000` | `4.000` | `0.085 N` | `1.963 N` | `1.000` | `23.796 mm` | MET |

- handover：MAKE recovery `184 ms`，zero-contact `0`，max force `3.474 N`，`MET`；
- 24-episode robustness：success `95.83%`，continuity `99.989%`，aggregate RMSE
  `0.197 N`，max force `3.069 N`，`MET`。

## 长程二维连续段结果

突变前 `[0.75,10.0) s`：

| 指标 | 值 | 门槛 | 判定 |
| --- | ---: | ---: | --- |
| path length | `406.758 mm` | `>=300 mm` | MET |
| hand contact probability | `1.000` | `>=0.995` | MET |
| average contacts | `3.930` | `>=3.50` | MET |
| thumb contact | `0.9788` | `>=0.95` | MET |
| contact-to-head clearance | `14.046 mm` | `>=10 mm` | MET |
| force RMSE | `0.699 N` | `<=0.50 N` | NOT_MET |
| force violation probability | `0.00270` | `<=0.005` | MET |
| maximum force | `18.308 N` | `<=8 N` | NOT_MET |

三个最大主曲率 bin 均被实际覆盖：low `15468`、high `2887`、extreme `145` pad samples。
extreme `>=40 1/m` bin 的 contact retention 为 `1.000`，但 force RMSE `1.021 N`、peak
`8.484 N`，对应力阈值 `NOT_MET`。

## 4 mm 突变与恢复

`t=10.0 s` object 瞬时远离 `4 mm`：

- pre-step contact set：`{1,2,3,4}`；immediate：`EMPTY`；
- any-contact sustained recovery：`66 ms`；
- per-finger recovery：`[70,66,68,94] ms`；
- all-finger sustained recovery：`94 ms`；
- longest zero-contact：`66 ms`；
- first post-step second peak：`118.900 N`；
- force settling：`1.966 s`，超过 `0.75 s`；
- final contact set：`{1,2,3,4}`。

`extreme_surface_recovered.png` 取首次四指恢复阶段，明确显示接触已经恢复但 force 仍过大；
`extreme_surface_force_settled.png` 取约 `t=12.03 s` 的稳定阶段。快速恢复不能覆盖过力和
settling threshold 为 `NOT_MET`，因此综合性能判定为 `EXTREME_THRESHOLDS_NOT_MET`；评测
本身仍为 `COMPLETED`。

## 可视化与文件验收

- 正式入口：`Module/generated/e05_physics_v3/index.html`；
- geometry audit、2D height/curvature/S-track dashboard、contact raster 和阈值判定均已人工
  检查；
- extreme MP4：`960 x 720 @ 30 fps`，`15.000 s / 450 frames`；
- 其余四段：`960 x 720 @ 30 fps`，各 `3.000 s / 90 frames`；
- HTML `27` 个本地资源引用全部存在；
- 原始 trace 位于 `traces.npz`，所有图和视频均由该正式 trace 重放生成。

## 测试

```text
Ran 32 tests in 23.051s
OK
```

测试包括 mesh-registered parent、pad size/head gap、拇指标称 normal、only-four-pad collision、
非网格 hfield ray audit、真实 handover、15 秒长程恢复/阈值判定以及 M01–M03 回归。

## 结论与边界

三个用户指出的展示/几何问题已经得到可执行且可视化的修正：拇指由真实关节角朝下接触，
其余三指只能由近端指腹 pad 接触，主序列扩展为 15 秒二维 multi-scale traversal。新的
控制结论仍是否定的：MCC 可以维持/恢复多指接触，但在该长程极端场景中无法满足 force
safety 与 settling。

掌部固定、object 采用反向 mocap；本实验不验证动态 wrist、non-tip collision、Finger DP
或 DP vs. MCC。
