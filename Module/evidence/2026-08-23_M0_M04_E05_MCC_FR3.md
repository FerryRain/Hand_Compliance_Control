# M0–M4 FR3+Leap 与 MCC-only E05 证据记录

## Scope

- 日期：`2026-08-23`
- 环境：`handcomp`
- Python：`/home/ferry/data/Anaconda/envs/handcomp/bin/python`
- 实现范围：M0-FR3、M01-FR3、M02-FR3、M03-FR3、M04-R/W/C/H
- 正式评测：`E05-F-MCC`、`E05-H-MCC`
- 明确排除：Finger DP 的实现、训练、推理和评测

## 已完成模块

| 模块 | 证据 | 状态 |
| --- | --- | --- |
| M0-FR3 | `fr3-leap-modules.v1`；7 arm + 16 finger；wrench frame/sign/source；JSONL round-trip | `PASSED` |
| M01-FR3 | live FR3 capsules；Oracle clearance；`mj_geomDistance` pad/object witness | `PASSED` |
| M02-FR3 | moving-wrist 四指 wrapper；signed coordinated force error；transition reset | `PASSED` |
| M03-FR3 | arm/per-finger stall；wrench/torque/limit/sensor/saturation guards；local/global action | `PASSED` |
| M04-R | exact `(nq,nv,nu)=(23,23,23)` FR3+Leap；fixed object；四个 distal belly pads | `PASSED` |
| M04-W | palm pose IK；joint-torque wrench estimator；6D Wrist MCC | `PASSED` |
| M04-C/H | `H_A=G_A B_A` resultant/internal decomposition；actual-contact authority；整手集成 | `PASSED` |

模型审计记录：

- arm DoF `7`，hand DoF `16`，actuators `23`；
- object mocap id `-1`，fixed object `true`；
- 四个 pad 的 parent 分别为 `fingertip`、`fingertip_2`、`fingertip_3`、
  `thumb_fingertip`；
- home pose 四个 pad world-Z normal 均小于 `-0.99995`；
- 正式 E05 gravity 为 `0 m/s^2`，仅用于隔离 contact controller。

## Protocol 与 provenance

- protocol：`Module/E05_MCC_FR3_PROTOCOL.md`
- protocol SHA-256：
  `483bd7ccf98ad5afec9bfb2d2912d2b9882d2a4045a194582b879b8adc51664a`
- formal code bundle SHA-256（完成后重新计算并与 summary 一致）：
  `6455d5d9018af4523e3576356b935e8aa393b5d1e9e691c545beb6c8ee7551ec`
- summary 记录 commit：`35d16159aaed9609511123f2183a2940147ad72a`
- summary 记录 worktree：dirty；本次没有覆盖用户的无关修改或删除。

正式场景每个 cell 有 3 个 paired episodes：nominal、low-friction、noisy-pose。每段
`15 s`，包含 `180 mm` nominal Y traversal、二维 S 型 X motion、连续多尺度曲率和
`t=9 s` 的 `4 mm` 突然离面恢复。

## Commands

正式评测：

```bash
/home/ferry/data/Anaconda/envs/handcomp/bin/python \
  -m Module.module_4_whole_hand_mcc.demo
```

视频与 gallery：

```bash
/home/ferry/data/Anaconda/envs/handcomp/bin/python -m Module.fr3_visual_demo
/home/ferry/data/Anaconda/envs/handcomp/bin/python -m Module.visual_demo
```

完整回归：

```bash
/home/ferry/data/Anaconda/envs/handcomp/bin/python \
  -m unittest discover -s Module/tests -v
```

结果：`Ran 49 tests in 31.528s — OK`。

## 正式结果

| aggregate 指标 | E05-F-MCC | E05-H-MCC |
| --- | ---: | ---: |
| execution | `EVALUATED` (3/3) | `EVALUATED` (3/3) |
| performance | `NOT_MET` | `NOT_MET` |
| mean contact continuity | 0.999905 | 0.999905 |
| mean average contacts | 3.7501 | 3.7101 |
| mean force RMSE | 0.7732 N | 1.0169 N |
| worst peak force | 11.0812 N | 13.1945 N |
| mean force settling | 0.2840 s | 2.3093 s |
| worst force settling | 0.3340 s | 5.9980 s |
| mean Y traversal | 0.173887 m | 0.175330 m |
| mean controller P95 | 1.249 ms | 1.263 ms |
| mean wrist Fz RMSE | — | 1.9837 N |
| mean internal leakage P95 | — | 0.01345 N |

F 未满足：`deadline_miss_probability`、`force_violation_probability`、`max_tip_force_n`。
H 未满足：`force_rmse_n`、`force_settling_s`、`max_tip_force_n`。

这里必须保持“执行有效”和“性能达标”分离：两个 cell 都是
`EVALUATED / NOT_MET`，不是 `FAILED`。H 的 traversal 略大、internal leakage 较小，但
force RMSE、peak 和 settling 没有优于 F，因此不能声明 H 已经更好。

## 可视化与数据产物

- `Module/generated/e05_mcc_fr3_v1/summary.json`
- `Module/generated/e05_mcc_fr3_v1/episodes.csv`
- `Module/generated/e05_mcc_fr3_v1/base_traces.npz`
- `Module/generated/e05_mcc_fr3_v1/generated_fr3_leap.xml`
- `Module/generated/e05_mcc_fr3_v1/model_audit.json`
- `Module/generated/visual_demo/index.html`
- `Module/generated/visual_demo/fr3_leap_mcc_dashboard.png`
- `Module/generated/visual_demo/fr3_leap_e05_f_mcc.mp4`
- `Module/generated/visual_demo/fr3_leap_e05_h_mcc.mp4`

两段 MP4 均为 H.264、`960x540`、`12 fps`、`180` frames、`15.000 s`：

- F video SHA-256：
  `29943b24a8fc1f8ee5d302bad88d28edf6a750858f25b8ed24c0a47501972481`
- H video SHA-256：
  `6c218fac378db46778413b30c17771baf02babf2bae891d36c94eee2899e1c8f`

## Gate

MCC 子门已评测但性能 `NOT_MET`；DP 子门按用户要求 `DEFERRED`。完整 G1 保持 No-Go，
不自动进入 M06/planner 集成。若继续优化 MCC，必须先冻结 v2 protocol，不能回写本次
threshold、trace 或 verdict。

## 提交前复核发现

`palm_lower` 是 `fr3v2_link8` 的固定 child，控制和动力学拓扑并未断开；但 v1 mount 使用
`[0,0,0.08] m` 的不可见刚性偏移，且没有 adapter geometry，造成用户指出的明显空隙。
因此本记录中的 E05 数值标记为“mount 修复前基线”。修复后必须生成新的 protocol/code
hash、trace、summary 和视频；不得把 v1 结果冒充修复后的结果。
