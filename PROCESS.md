# 主分支开发进程（持久检查点）

> 本文档是跨 Codex session 的主入口。任何 session 开始工作前，应先读本文档，
> 再读对应子项目的 `PROCESS.md` 和链接的 GitHub issues。每完成一个阶段都要更新并
> 提交本文档；不要只在任务结束时补写。任何上下文压缩/摘要恢复发生后，也必须
> 重新读取根与子项目两份 PROCESS，不能只依赖压缩摘要继续。

> 研究主线首先以 [`PROPOSAL.md`](PROPOSAL.md) 为准：核心贡献是“可规模化
> 逆向示范生成 + wrist-conditioned finger DP + wrist-only ER-GPIS”。
> `full_hand_mcc/` 的显式腕部/多指联合优化属于 Baseline 2；当前 MCC 是该
> baseline 的底层控制器，不是主方法的在线高维规划器。

## 用户要求（必须保留在文档头部）

1. 以后只在 `main` 更新，不再切换或创建 Codex 功能分支。
2. 主 README 只提供项目目录索引、简要运行命令和代码结构；子目录 README 说明
   具体问题、实现和用法。
3. 仓库根目录和子项目目录都要有 process 文档，记录已完成、未完成、正在做、
   遇到的问题、失败方法、下一步和相关 issue，便于其他 session 快速续接。
4. `full_hand_mcc` 使用 Franka FR3 + LEAP Hand：机械臂关节负载作为掌根 MCC
   反馈，16 个手指电机负载作为四个指尖 MCC 反馈。
5. 测试输入为掌根与四个指尖共五点；实际关节状态必须满足真实 URDF/MJCF
   关节约束和同时可达性。2026-07-28 用户进一步澄清：四个物理指尖表面目标是
   硬约束；掌根点只作为机械臂/手掌姿态的软引导，不要求接触物体，也不再要求
   3 mm 硬跟踪。掌根偏差仍要记录并设置防漂移上限。
6. 物理指腹必须沿表面缓慢滑动。2026-07-29 用户放宽逐帧要求：允许短暂少于
   3 指、甚至短暂零指接触，也允许短暂停止位移；但连续失联/停顿必须受限，
   在整段移动的大部分时间必须由大部分手指（目标至少 3 指）保持物理指腹接触，
   每指接触率、平均同时接触数和多数接触帧占比均需终审，路线末段必须恢复
   四指稳定接触。不能自然闭合，不能由指甲/手背冒充指尖接触，不能穿模。
7. 路径要从物体下端移动到上端并进入顶部曲面区域；不能只移动很短距离。
8. 最终碰撞语义分为两类：FR3 机械臂的所有链节严格禁止接触物体，规划优化中
   必须显式加入机械臂/物体位姿和净距约束，运行时任何 FR3/物体接触都立即判失败；
   LEAP Hand 的掌部、指节、指背等所有位置允许受控接触物体，但只有四个物理
   fingertip pad 可计入指尖接触目标。其它手部接触不能冒充指尖接触、不能把指尖
   顶离表面，也不能产生过大接触力或明显穿透；四指尖仍按“整段大部分时间
   至少三指、短暂失联后恢复、末段四指稳定接触”独立验收。
9. 可探索多种 MCC、轨迹优化、MPC 等方法；每种结果都必须审核。继续研究改进
   方法，不局限于预先规定的数量。
10. 要在曲率变化明显和多个物体上验证泛化性；若泛化不足，明确记录，并作为
    后续使用 DP 学习指尖跟随的依据。
11. 允许反复运行 Windows GPU MJLab。最终视频必须由 Codex 自己逐帧/抽帧审核，
    审核不合格不得交付。
12. 新要求和问题要及时写入 GitHub issue；持续处理已有 issue。成功后直接提交
    并推送 `main`。
13. 2026-07-28 用户决定在当前 demo 完成前继续使用此前已经验证的项目
    `.venv`（其基础解释器来自 `DMtactile`），暂不切换完整运行栈到 `handcomp`；
    项目完成后再通知用户并提供手动配置 `handcomp` 的步骤。
14. 视频必须按阶段保存：冒烟/探针、历史方法调试、FR3 规划调试、已审核参考和
    FR3 完整交付分别放入固定子目录。失败或未审核视频不得进入交付目录，已有视频
    也要按同一规则重新分类。
15. 2026-07-29 项目研究定位以根目录 `PROPOSAL.md` 为准。主贡献改为可规模化
    逆向示范生成、以 wrist motion/触觉为条件的 finger Diffusion Policy，以及
    wrist-only ER-GPIS 主动探索。在线主方法不求解高维全手非线性优化；
    `full_hand_mcc/` 是“方案二 / Explicit Whole-Hand Optimization Baseline”，
    当前工作只实现其底层 MCC、Oracle 和后续 time-capped 对照，并可作为逆向
    示范候选轨迹/teacher 的来源。

## 仓库状态

- 运行仓库：`D:\Code\Hand_Compliance_Control\mjlab_full_hand_mcc`
- 当前分支：`main`
- `codex/full-hand-mcc-end-to-end` 已于 2026-07-25 快进合并并推送到 `main`
  （合并后提交为 `990c99a`）。
- 后续不得为本项目创建新分支；允许在 `main` 上分阶段提交持久检查点。

## 子项目进度

| 子目录 | 作用 | 状态 | 详细进程 |
| --- | --- | --- | --- |
| `full_hand_mcc/` | Baseline 2 的 FR3 + LEAP 全手 MCC、五点显式优化、碰撞审核、视频 | 基线迁移进行中 | [`full_hand_mcc/PROCESS.md`](full_hand_mcc/PROCESS.md) |
| `palm_compliance_control/` | 原始掌部/机械臂 MCC 参考 | 已有参考实现 | README/源码 |
| `mcc_finger_compliance_control/` | 手指 MCC 参考 | 已有参考实现 | README/源码 |
| `finger_compliance_control/` | 手指顺应控制参考 | 已有参考实现 | README/源码 |
| `minimalist_compliance_control/` | 上游/简化 MCC 方法 | 参考代码 | README/源码 |

## GitHub issue 索引

- [#8 主方法：逆向示范生成 + wrist-conditioned finger DP + wrist-only ER-GPIS](https://github.com/FerryRain/Hand_Compliance_Control/issues/8)
  —— Proposal 主线与 Phase 1–5 的实现/实验追踪；优先级高于继续扩展在线联合优化。
- [#7 FR3 full-hand MCC：迁移机械臂并滑到物体顶部](https://github.com/FerryRain/Hand_Compliance_Control/issues/7)
  —— Baseline 2 的底层 MCC、Optimization Oracle 和 time-capped 对照。
- [#4 多物体/曲率泛化](https://github.com/FerryRain/Hand_Compliance_Control/issues/4)
  —— 未完成，FR3 单物体验收后继续。
- [#1 硬件力矩符号与五接触接口](https://github.com/FerryRain/Hand_Compliance_Control/issues/1)
  —— 仿真后续硬件问题，仍开放。
- [#3 长距离连续表面滑动](https://github.com/FerryRain/Hand_Compliance_Control/issues/3)
  —— xArm6 版本已关闭；FR3 的更强顶部要求由 #7 追踪。
- [#6 指腹方向、全机碰撞和长路线审核](https://github.com/FerryRain/Hand_Compliance_Control/issues/6)
  —— xArm6 验收记录，已关闭。

## 当前正在做

2026-07-28 完成视频目录治理：将 `full_hand_mcc` 的 259 个历史 MP4 从
`output/` 与 `outputs/` 根目录迁移到阶段子目录，其中冒烟/探针 77 个、
历史表面方法调试 106 个、FR3 规划调试 75 个、已审核 xArm6 参考 1 个。
`outputs/deliverables/fr3/` 当前没有合格视频，保持为空；v74 及后续失败运行必须
直接写入 `outputs/debug/20_fr3_planning/`。运行环境按用户最新决定继续使用旧项目
`.venv`；`handcomp` 完整迁移推迟到项目结束后，由用户按最终清单手动配置。

2026-07-28 用户澄清最终碰撞规则：FR3 必须严格零接触；LEAP Hand 全部几何允许
受控物体接触，但非 fingertip pad 接触不计入四指接触，且不能破坏至少三指支撑和
末段四指恢复。此前把掌部/非指尖链节统一纳入 2 mm 禁碰净距的规则已经废止。
v71 在 79.6 mm 前的停止属于旧规则误判：最佳掌部距离仍为正的 1.927 mm，
并非已经发生物理碰撞。当前先拆分 FR3 与手部几何/传感器集合：FR3 保留严格规划
净距与运行时零接触；手部改为允许接触但限制力和穿透，并继续用四个独立 fingertip
传感器验收三/四指支撑。实现已将 8 个 FR3、13 个非指尖手部和 4 个 fingertip
碰撞几何分成互斥集合；FR3 用最近距离/运行时接触严格终止，手部用最近穿透与
最大力两组传感器审核。默认手部附带接触上限为 1 mm 穿透、单几何 24 N、合计
36 N，均可由命令行调严；附带接触期间若 fingertip 少于要求的三指则立即失败。
新增几何/传感器回归后 12/12 测试通过。原 v72 的 385 帧加密只为解决已经废止的
2 mm 掌部正净距，因此不先消耗该计算；v73 先用 v71 的 193 关键帧复现并继续全程，
只有出现真实 FR3 净距、手部过深穿透或插值平滑问题时再加密。v73 已通过旧瓶颈
79.6 mm：FR3 净距约 97.6 mm、手部最近距离仍为正的 0.90 mm、保持 3/4
规划指尖支撑。82.1 mm 的新首项为重定位食指沿面进度误差 6.92 mm；不是碰撞。
v74 将三支撑/重定位窗延到 100 mm、瞬态进度带提高到 7.5 mm 后已经通过
82.1 mm；84.6 mm 的新首项仅为食指切向误差 3.48 mm，而从 82 mm 开始的
恢复曲线已收紧到 3.47 mm。该帧 FR3 净距约 98.2 mm、手部最近距离仍为正的
1.57 mm，并保持 3/4 支撑。v75 不放宽 3.5→3 mm 的切向端点，只把进度/切向
恢复起点与 85 mm 法向落指阶段对齐，已连续通过 92.0 mm。下一帧约 94.5 mm
首次失败变为 4.415 mm 掌根球误差，局部
`[法向 -2.050, 方位 +0.263, 经线 -3.901] mm`；FR3 净距约 96.3 mm，
手部穿入约 0.55 mm 且保持 3/4 支撑。当前新增第二阶段局部掌根目标增量：
第一段 77.5–80 mm 修正保持不变，第二段只能在第一段完全建立后生效，并强制
合成输入仍在 3 mm 球内。v76 从 92–94.5 mm 只加 +0.5 mm 经线扰动测量局部
响应，但显示为 92.0 mm 的第 37 帧实际距离略大于 0.0920 m，极小非零相位
已经触发非凸分支切换；食指法向误差 4.98 mm 超过该帧 4.92 mm 恢复带，
而 v75 原分支同帧只有 3.75 mm。v77 将第二段起点严格门控到 92.1 mm，
斜坡设为 2.4 mm，保证已验证帧相位精确为零并在 94.5 mm 完整建立扰动；
100 mm 后仍恢复四指名义接触。v77 已按预期复现并通过 92.0 mm；在 94.5 mm
第二段 +0.5 mm 经线修正完全建立后，掌根实际点相对目标的误差从 v75 的
4.415 mm 降到 4.202 mm，局部误差由
`[-2.050,+0.263,-3.901] mm` 改善为 `[-2.070,+0.075,-3.656] mm`。
同帧 FR3 净距约 96.29 mm、手部最近穿入约 0.55 mm、保持 3/4 指腹支撑，
因此仍是掌根五点跟踪精度失败，不是 FR3 碰撞或指尖支撑失败。这里的 3 mm
掌根可行球约束实际掌根控制点与规划目标点的三维距离，不表示掌根必须距离物体
表面 3 mm。随后用户明确允许放宽该约束：四个物理指尖到达表面才是硬目标，
掌根只作软引导。用户继续明确掌根应随 FR3/MCC 被动移动，只需给出大致方向；
因此新增 `--palm-guide-only`：取消掌根精确位置的硬惩罚与普通失败判据，仅保留
0.25 倍弱方向/位置引导和 30 mm 纯安全防漂移界限，实际误差继续完整记录。
FR3 零接触、指尖误差、至少三指支撑和手部受控接触阈值均不变。v78 曾在旧
3 mm 跟踪球下把第二段经线目标改写提高到 +2.0 mm（两段合成 2.994 mm），
但 94.5 mm 食指法向误差恶化到 5.55 mm，超过该帧 4.06 mm 恢复带，说明
大幅移动掌根目标会损害指尖回落，故拒绝并恢复 v77 的 +0.5 mm 小修正。v79
启用被动掌根后通过旧 94.5 mm 瓶颈：掌根误差 5.46 mm、3/4 指腹；97.0 mm
恢复 4/4 指腹，FR3 净距约 95.72 mm。99.5 mm 的新首项仅为四指沿面进度误差
`[4.28,4.23,3.21,3.24] mm`，前两指略超不变的 4 mm 恢复末端阈值。
v80 不延长恢复窗口、不放宽指尖阈值，新增两级 1600/2400 进度优先修复权重，
让求解器牺牲掌根精度而不是指尖严格规划。v80 仍收敛到与 v79 完全相同的
99.5 mm 局部解，证明单纯加权无效。v81 保持全部硬阈值与被动掌根不变，把
0.48 m 路线从 193 加密为 385 个关键帧、每段约 1.25 mm，以更密的严格指尖
表面目标和连续 URDF 可达求解跨过该局部瓶颈。v81 在 99.7 mm 严格失败，
四指进度误差为 `[4.05,3.70,0.81,4.05] mm`；食指/拇指各只超固定
4.00 mm 门槛 0.05 mm，相比 v79/v80 已明显改善。失败前 4/4 指腹恢复、
FR3 净距约 96 mm、掌根漂移约 6.3 mm，故问题仍是指尖进度分配而非掌根或
碰撞。v81 视频只保留在调试目录。下一版不放宽阈值，改为在 385 基础网格上
只对 90–110 mm 恢复末端做二倍局部加密，并用真实表面距离插值动力学帧。
v82 实现已完成：新增局部加密起止距离与倍率参数，385 基础网格只增加到
402 个关键帧；瓶颈区约 0.623 mm/段、其它区域约 1.247 mm/段。4700 步轨迹
改为按 `coarse_distance` 的真实表面距离查区间插值；NPZ 保存完整网格元数据。
AST、CLI、网格断言和 12/12 MCC/碰撞回归测试通过。v82 GPU 在 98.5 mm
因四指进度 `[3.13,4.15,2.79,3.67] mm` 严格失败；同帧仍有 4/4 指腹、
FR3 净距约 96.46 mm、手部穿入约 0.28 mm、掌根漂移约 6.0 mm。由此确认
从 90 mm 加密会过早改变非凸支路。v83 保留 v81 到 98.5 mm 的可行前驱，
只从 98.6 mm 起细分跨向 99.7 mm 的首次不可行区间，阈值不变。v83 精确
通过 98.5 mm 可行前驱后，新增 99.1 mm 半步仍因进度
`[4.20,3.54,0.66,3.87] mm` 拒绝；同期法向/切向与 4/4 接触均已恢复。
因此 v84 把 100 mm 的物理接触恢复终点和进度相位同步终点解耦：前者不变，
后者延长到 102 mm，在保持 4/4 指腹滑动期间继续收紧并最终恢复固定 4 mm。
实现新增 `--transient-progress-recovery-end-m`，只改变进度相位曲线并写入
NPZ；99.1/99.7/102 mm 的进度带数值为 4.271/4.175/4.000 mm。AST、CLI、
12/12 MCC/碰撞回归和数值断言通过。v84 GPU 因曲线从 85 mm 起就改变支路，
在 98.5 mm 得到掌部附带穿入 1.041 mm（上限 1.000 mm）的候选，FR3 净距
仍约 95.53 mm；严格拒绝。v85 恢复 v83 原进度曲线，只在 98.55–102.5 mm
给食指规划一个最大 0.4 mm、首尾为零的经线目标相位凸包，保持 98.5 mm 前
完全不变，并在 102.5 mm 回到原同步目标和固定门槛。v85 实现已完成：
新增独立终端修正幅值、窗口和四指 scale 参数，明确重写每根指尖的表面计划点，
不改变验收带。数值检查在 98.4935/99.1169/99.7403/100.525/102.5 mm
得到食指目标偏移 0/0.174302/0.324595/0.400000/0 mm；AST 和完整
12/12 MCC/FR3-手部碰撞策略回归测试通过。v85 GPU 精确保留并通过
98.4935 mm 可行前驱，在 99.1169 mm 把 v83 食指进度误差从 4.20 mm
降到 4.09 mm；该帧活动收紧带为 4.034967 mm，因此仍约差 0.055 mm，
按硬规则拒绝。同期其余三指为 3.46/0.59/3.82 mm、4/4 指腹、FR3 净距
约 96.45 mm。v86 不放宽阈值，按 v83→v85 实测响应把同一零端点凸包幅值
从 0.4 mm 提到 0.7 mm；99.1169 mm 目标偏移由 0.174298 增到
0.305021 mm，仍保持 98.5 mm 前和 102.5 mm 后严格为零。v86 已通过
99.1169、99.7403、100.3636、100.9870 和 101.6104 mm，均为 4/4
物理指腹；FR3 净距保持约 96.5–96.9 mm。新首项移到 102.2338 mm：
食指进度 4.02 mm，仅超 100 mm 后固定 4.00 mm 门槛约 0.02 mm；
其它三指为 3.71/0.52/3.55 mm。该帧 v86 目标凸包已衰减到
0.147117 mm。v87 按同一实测响应把峰值提高到 0.9 mm，使该帧目标偏移
为 0.189151 mm；窗口、零端点、指尖带和碰撞阈值均不变。v87 暴露响应
非单调：统一放大后在 99.7403 mm 提前失败，食指进度 4.02 mm，而其它三指
为 3.21/0.23/3.57 mm；4/4 指腹和 FR3 净距仍正常。v88 不再统一放大，
恢复 v86 的 0.7 mm 主凸包，并新增只在 101.7–103.5 mm 生效的 0.06 mm
食指尾段半正弦凸包。它在 99.7403 mm 严格为零，在 102.2338 mm 提供约
0.048 mm 额外目标偏移，并在 103.4805 mm 已衰减到约 0.002 mm。v88
实现已完成：新增独立尾段幅值、窗口和四指 scale 参数并写入 NPZ；AST、CLI、
实际网格偏移断言 `0/0/0.048154/0.002040 mm` 和完整 12/12 回归测试通过。
v88 按预期通过 102.2338 和 103.4805 mm，均保持 4/4 指腹、FR3 净距
约 96.7 mm；新首项移到 104.7273 mm 并换成中指进度 4.08 mm，食指已经
降到 3.45 mm，其余为 0.79/3.74 mm。v89 将局部相位修正泛化为可重复的
每指半正弦基函数；保留 v88 两段食指计划，并只在 104.0–106.5 mm 给中指
添加约 0.16 mm 峰值的局部计划点相位。v89 实现已完成：新增可重复
`--finger-meridian-local-phase`，每项包含幅值、起止距离和四指 signed scale，
并保存到 NPZ。实际关键帧 103.4805/104.7273/105.9740/107.2208 mm 的中指
偏移为 0/0.126704/0.098220/0 mm；AST、CLI 和完整 12/12 回归测试通过。
v89 在 104.7273 mm 将中指进度从 v88 的 4.08 mm 降到 3.87 mm，但耦合
分支把食指从 3.45 mm 推到 4.12 mm；其余为 0.55/3.78 mm，仍为 4/4
指腹且 FR3 净距正常。v90 不改实现或阈值，在同一 104.0–106.5 mm 基函数中
使用食指/中指 scale `1.5/1.0` 联合重相位，对两个耦合自由度同时修复。
v90 已通过 104.7273、105.9740 mm，并在 107.2208 mm 被固定 4.00 mm
进度门槛严格拒绝：四指进度误差为 `[4.17,2.92,0.29,1.98] mm`，4/4
物理指腹仍接触，FR3 净距约 97.3 mm，手部附带穿入约 0.15 mm。失败发生在
104.0–106.5 mm 相位刚归零后的首个关键帧，因此 v91 保留 v90 已验证相位，
再拼接一段 106.0–109.0 mm、峰值 0.14 mm、仅作用于食指的半正弦尾相位；
它在 105.9740 mm 严格为零，在 107.2208 mm 提供约 0.134 mm 目标前移。
v91 已把 v90 的 107.2208 mm 首个失败点修复，并继续连续通过
108.4675、109.7143、110.9610、112.2078 和 113.4545 mm；这些帧均为
4/4 物理指腹，FR3 净距约 96.9–98.0 mm。新首项在 114.7013 mm：
四指进度误差 `[3.94,4.01,1.86,2.33] mm`，仅中指超过固定 4.00 mm
门槛约 0.01 mm，食指为 3.94 mm；同帧 FR3 净距约 96.9 mm，手部最近
距离约 -0.01 mm。v92 保留前三段相位，新增 113.5–116.0 mm、
中指峰值 0.05 mm、食指/中指 scale `1.5/1.0` 的耦合短相位；它在
113.4545 mm 严格为零，在 114.7013 mm 约为 0.075/0.050 mm。
v92 已通过 114.7013 和 115.9481 mm，保持 4/4 指腹、FR3 净距约
96.5–96.9 mm；但在相位归零后的 117.1948 mm 同步换到不可行支路，四指
进度误差扩大为 `[2.67,4.94,4.23,5.03] mm`。该帧的表面目标间距仍为
1.247 mm，因此 v93 不再继续手工堆相位，而是把 MPC 加密泛化为可重复窗口：
新增 `--mpc-local-refine-window START_M END_M FACTOR`，重叠窗口取最大
细分倍数，旧单窗口参数保持兼容，全部窗口及实际网格写入 NPZ。v93 保留原
98.6–101.5 mm 二倍窗口，再在 115.95–130 mm 二倍加密；实际网格为
400 帧，并在 115.9481/117.1948 mm 之间插入 116.5714 mm 可达 shooting
点。AST、CLI、严格单调/0.48 m 端点网格断言和 12/12 MCC/接触策略回归通过。
v93 的新增 116.5714 mm 半步将 v92 在 117.1948 mm 的三指超差改善为
`[3.88,3.71,2.96,4.16] mm`，只剩拇指超过 4.00 mm 约 0.16 mm，
但加密本身仍不足以跨过耦合支路。v94 实现失败触发的 bounded joint
rephasing shooting：新增最大偏移、最小搜索步长、逐帧衰减和近门槛耦合边界
四个参数；普通与原修复求解都失败后，才对近门槛手指的单指/联合 signed
目标相位做倍增搜索。偏移逐关键帧连续衰减，并在最后 20 mm 内强制平滑归零；
候选仍必须同时通过原进度、法向、切向、至少三指/末段四指、关节步长、
FR3 净距、手部穿入、自碰撞和指腹角硬约束。关键帧偏移及配置保存到 NPZ。
AST、CLI、12/12 回归和 diff 检查通过；v94 使用最大 0.4 mm、步长
0.05 mm、衰减 0.02 mm、近门槛边界 0.5 mm。
v94 首次验证自动方法有效：在 116.5714、117.1948、117.8182 mm 分别
接受自动偏移 `[0.05,0,0,0]`、`[0.03,0,0,-0.05]` 和
`[0.01,0,0,-0.40] mm`，把四指进度维持在固定 4 mm 内且保持 4/4
指腹。118.4416 mm 的新首项不再是进度，而是最差指腹角数值上达到 50.00°；
FR3 净距仍约 95.7 mm，手部穿入约 0.933 mm，均通过各自硬阈值。v94 的
自动 shooting 当时只由进度失败触发，因此没有机会修复指腹角。v95 将触发
扩展为任一硬约束失败；若当前进度已合格但法向/切向/接触/关节步长/碰撞/
指腹角失败，则联合搜索四指相位，每个候选仍按全部原门槛验收。AST、12/12
回归和 diff 检查通过。
v95 首次 GPU 检查暴露实现语义错误：它把相邻粗 MPC shooting 关键帧的关节
差误当成 `--max-plan-joint-step-rad`，导致 3–12 mm 已验证前段被不必要地
连续重相位并停顿。该参数原本且最终仍严格检查插值后的运行帧关节步长；粗关键帧
之间会被数十个运行帧插值，不能使用同一门槛。v95 被主动终止且视频不得交付。
v96 已从自动触发和候选局部验收中移除粗帧关节差，保留最终插值运行帧的原硬检查；
自动触发仍覆盖进度、法向、切向、接触、单调性、掌根安全、FR3/手部碰撞、
自碰撞和指腹角。AST、12/12 回归和 diff 检查通过。
v96 正确复现前段且没有提前触发，并自动修复 v94 的 118.4416 mm 指腹角：
接受 `[-0.05,0,0,-0.38] mm` 后四指进度为
`[3.52,3.52,2.12,3.61] mm`、4/4 指腹。随后推进到 124.0519 mm；
124.6753 mm 的新首项为 `[4.03,4.03,2.31,4.08] mm`，0.4 mm 搜索
范围内没有同时通过全部硬约束的候选。运行记录显示固定每帧衰减 0.02 mm 会在
上一帧仍贴近 4 mm 门槛时先撤掉有效相位，再重新 shooting。v97 改为带滞回
衰减：只有上一关键帧所有指尖进度进入门槛内额外 0.5 mm 的安全区才向零衰减；
贴边时保持偏移。GPU 搜索上限提高到 0.8 mm（仍小于 1 mm），最终 20 mm
平滑归零和全部硬阈值不变。AST、12/12 回归和 diff 检查通过。

正在执行 #7：FR3 23 DoF 迁移与 GPU smoke 已完成；底端 v4 抓取已经通过静态审核。
v4c–v32 的候选均被严格验收拒绝。v24–v27 证明继续提高统一修复权重或增加掌根倾斜
会在指尖法向、2 mm 全机净距和 50° 指腹角之间转移违规，不能解决耦合可达性。
v28 起把掌根沿“掌根目标自身的物体局部法向”外移，而不是沿四指平均法向；该方法
明显扩大下端曲率过渡区的可行域。v30 曾到达 39.8 mm，但重复实验暴露环境 seed
原为 `None`，而且 GPU 加载接触状态的微小漂移会让非凸 MPC 选择不同分支。现已增加
`--seed`、单线程 BLAS 和可选的加载状态量化锚点。v33/v33b 表明相对实时伺服命令
量化仍不稳定，因为实时命令本身依赖 GPU 状态；现改为相对资产中的固定初始抓取量化。
用户同时把接触验收更新为显式三支撑：允许一个手指短暂离面，但至少三指始终接触且
末段四指恢复。v34 已证明 27.4–37.3 mm 可连续保持三指支撑，但 39.8 mm 的掌部
避障只剩一指接触；把 3 mm 掌根球扩展到切向/经线方向后，v35 已找到碰撞安全的
三支撑候选，只剩 4.08–4.70 mm 经线进度误差。v36 使用窗口 5 mm 进度带后在
39.8 mm 仅剩无名指法向误差 3.22 mm；检查发现候选排序没有把进度/法向/切向
硬违规提升到与碰撞相同的淘汰优先级。v37 修复排序后在 39.8 mm 得到三支撑候选，
但掌部净距只有 1.372 mm；v38 把掌根球放大到 4 mm 后净距反而降到 0.696 mm，
证明额外平移自由度不是正确方向。v39 恢复 3 mm 掌根球并把曲面姿态运输从 70%
降到 65%，已在 39.8 mm 通过全部约束并保持 3/4 接触，但 42.3 mm 掌部净距
为 1.845 mm，仍按 2.000 mm 阈值拒绝。当前只把后段二次掌根外移从 5.3 mm
提高约 0.3 mm 的 v40 反而使 42.3 mm 净距降到 1.626 mm，证明继续平移不是
正确方向。v41 恢复 5.3 mm 外移并把姿态运输降到 62.5%，42.3 mm 已通过，
首次失败推进到 44.8 mm（掌部净距 1.545 mm）。当前沿已验证趋势运行 60% 姿态
运输的 v42 已通过 44.8 mm，在 47.3 mm 只差 0.107 mm 净距；v43 的 59%
姿态运输继续通过 47.3 mm，首次失败推进到 49.8 mm（净距 1.588 mm）。
v44 全程 57.5% 在 39.8 mm 前进入不同局部分支，掌根球误差 3.399 mm 超过
3.000 mm，证明不能全程继续降低。当前新增分段姿态增益：前段保持 v43 已验证的
59%，从 45 mm 起平滑降到 55%，已把碰撞瓶颈从 49.8 mm 推进到 62.2 mm。
v45 首个失败项改为食指进度 5.46 mm，因为食指步态到 70 mm 才归零，而原重定位
窗口在 60 mm 提前结束。当前把同一指定食指窗口延到 72 mm、窗口内进度带设为
6 mm 后，v46 已通过进度，但同一帧食指切向误差 3.02 mm 刚超过统一 3.00 mm，
其余三指为 0.00–1.70 mm。当前新增仅对窗口内指定食指生效的 3.5 mm 切向带；
支撑三指及窗口外四指仍为 3 mm。v47 推进到 69.6 mm；v48 延长恢复窗口并把
指定食指法向带设为 6.5 mm 后通过 69.6 mm，在 72.1 mm 只剩掌部净距
1.909 mm。v49 仅把晚段姿态增益从 55% 降到 54%，已消除该碰撞瓶颈并连续
通过 74.6 mm；77.1 mm 在平坦重定位窗口刚结束后食指进度误差仍为 4.97 mm，
超过恢复后的 4.00 mm 阈值。当前使用分相恢复调度：指定食指的
法向带由 6.5 mm 收紧到 3 mm、切向带由 3.5 mm 收紧到 3 mm、进度带由
6 mm 收紧到 4 mm；其余三指全程保持名义接触，85 mm 后四指全部恢复名义
接触。v58 用局部掌根目标修正 `[-0.5,-0.5,-1.0] mm` 后在 79.6 mm 的
掌根球误差为 3.880 mm；v59 将经线反向为 +0.5 mm 后误差降到 3.446 mm，
局部分量为 `[-1.892,0.004,-2.881] mm`，仍未通过 3 mm 球。三次受控扰动
表明此前绑定的法向/方位项不足以覆盖剩余误差。v60 使用
`[-1.5,0,+0.75] mm` 后把误差降到 3.036 mm，只差 0.036 mm。完整局部响应
矩阵的无约束解超过 3 mm 修正上限，当前 v61 改用 3 mm 球内约束最小二乘的
90% 保守点 `[-1.65,0,+2.13] mm` 后触发非凸分支跳变，误差升到 5.011 mm。
当前停止大步外推，回到只差 0.036 mm 的 v60 邻域。v62 的小步修正通过了
3 mm 掌根球，但首个失败转移为掌部净距 0.901 mm。当前 v63 不再移动五点
目标，而是在原 3 mm 掌根球内增加确定性的外法向+方位/经线组合多起点。v63
从早段启用新种子后在 74.6 mm 提前换支路，食指法向 6.51 mm 超过 6.50 mm。
v64 将新种子门控到 77.5 mm 后，成功保留早段并把首个失败从掌根球/碰撞移开；
79.6 mm 只剩指定食指进度误差 5.13 mm。v65 推迟恢复起点后解决进度首项，
但新分支掌根球误差为 3.235 mm。v66 的小步修正解决掌根球/碰撞后只剩食指法向
6.06 mm 超过恢复曲线 5.45 mm。v67 延后落指后只剩掌部净距 1.782 mm。
当前 v68 把末段局部法向目标从 -1.67 调到 -1.40 mm，向外移动 0.27 mm；
85 mm 仍完成名义四指恢复。碰撞、指腹角及自碰撞阈值均
不放宽。任何未完成全程数值与视频审核的输出不得交付。

已完成：

- 从 MuJoCo Menagerie 取得官方 FR3 v2 关节、惯量、关节限制和低多边形碰撞网格，
  保留 Apache-2.0 许可证与上游说明。
- 新增轻量 FR3 模型骨架，设计在 `fr3v2_link8` 法兰处刚性装配 LEAP Hand。
- 环境核心的 7/16/23 DoF 常量、FR3 关节命名、机械臂动作项、观测切片、掌根和
  手指负载反馈切片已完成改造；GPU 环境 reset 与单步 action 已通过。
- 掌根五点中的第 0 点改为 `palm_lower` 根部控制点，不要求物理接触。

当前阻塞/问题：

- MuJoCo 原生 XML 解析器不能可靠读取包含中文字符的模型路径。因此源码可在
  `D:\文档` 镜像中补丁化，但编译/GPU 运行必须同步到 ASCII 路径 `D:\Code`。
- 第一次动态装配测试发现 `MjSpec.attach` 默认会添加模型名前缀；已明确使用空
  前后缀保留既有指尖 body/site 名称。
- 第二次编译发现 attach 后相对网格路径丢失；正在把 FR3 与 LEAP 网格路径在
  合并前解析为绝对路径。该修复已完成。
- 2026-07-25 检查点：FR3+LEAP 合并模型已在 `D:\Code` 编译成功，
  `nq=23`、`nv=23`、28 bodies、25 geoms；23 个关节顺序和五点 site 均已确认，
  `FivePointReachabilitySolver` 输出 `(5, 3)`。
- FR3 法兰初始安装方向会让手指向后穿入 link6/link7，最深 49.27 mm；已在安装
  旋转后增加局部 X 轴 180° 翻转，home pose 的机械臂/手自碰撞降为 0。
- demo、抓取优化器、长路线分支搜索器和指腹映射诊断脚本已完成 23/7 参数化，
  10 个 MCC core unittest 全部通过。
- 已生成 `full_hand_mcc/assets/fr3_capsule_100x170_grasp_v1.npz`：
  指尖误差最大 0.043 mm，指腹角 13.65°–38.89°，非指尖最小净距 4.885 mm，
  自碰撞 0。
- 已生成高净空 v2 抓取与 0.48 m 路线。路线使四指全部进入上端帽面，静态路线
  机械臂最小净距 43.669 mm；240° 物体接近路径通过全机/物体净距审查。
- v2 GPU 接近阶段无非指尖/物体碰撞，但首个滑动关键帧发现
  `mcp_joint_geom`–`dip_geom` 动态自碰撞约 0.080 mm，超过 0.05 mm 验收阈值，
  已拒绝该视频。
- 无底端高度约束的 v3 虽有 0.581/0.751/0.607 mm 自碰撞余量，但两指的
  0.48 m 目标会越过胶囊顶点 24/71 mm，已由端点审计拒绝，没有提交该路线。
- 加入底端高度约束后的 v4 抓取通过：最大指尖误差 0.206 mm，非指尖/物体净距
  14.970 mm，受保护自碰撞净距 0.596/0.743/0.713 mm，指腹角
  26.53°–39.22°。
- v4c 路线通过 0.48 m 静态审计：物体底部约 6 mm 离地，四指终点均在上端帽面
  且距顶点 31.9–61.8 mm；FR3/物体最小净距 31.310 mm，最大相邻机械臂步长
  0.10642 rad。
- v4c 在 GPU 中暴露出固定/过早 18° 掌根倾斜会使指腹角迅速超过 50° 并造成
  手指纵向进度失败；该结果已拒绝。
- v5 把倾斜延迟到 60 mm 后，但在倾斜开始前固定掌根姿态已无法跟随下端帽面曲率；
  约 12.4 mm 后指腹角约 54°，因此拒绝。
- v6 改为根据四指局部表面坐标系运输掌根姿态，四指早期方向与进度明显改善；同时发现
  掌根是非接触控制点，不能用物体表面弧长约束。删除该错误约束后，掌部仍随曲面运输
  向物体摆入并发生碰撞，因此拒绝。
- v7 在当前表面法向增加 8 mm 掌根外移，静态 0.48 m 路线通过且机械臂最小净距
  76.002 mm、最大相邻机械臂步长 0.02044 rad；但 GPU 到约 25 mm 行程时掌部净距
  由正变负，四指纵向误差约 4.6–5.0 mm，因此仍拒绝。
- v8 把外移提高到 15 mm，静态机械臂路线通过，但四指耦合可达性在约 20 mm 失败。
- v9/v10 试验 70%/50% 曲面姿态跟随；70% 可推进到约 35 mm，50% 虽净距更大但
  四指在约 20 mm 即失去同步可达性，因此选择 70%。
- v11 加入 25–35 mm 二段外移；固定外移仍存在掌部避障与手指可达冲突。
- MPC 已增加 3 mm 非接触掌根球形可行域、严格插值审核和球内多起点搜索；
  掌根实际偏移被记录到计划资产。
- 已增加局部异步指尖弧长计划。食指在 20–70 mm 下端曲率过渡区使用平滑半正弦超前，
  窗口结束后归零，四指最终目标仍相同。v17 使用 8 mm 峰值后，在 34.8 mm 处
  四指法向误差 0.18–2.12 mm、掌根偏移 1.54 mm；37.3 mm 处仅剩
  0.155 mm 掌部净距缺口，指腹角 48.02°。
- v18 暴露出原约束修复不会由单独的掌根球/全机净距失败触发；现已显式加入
  3 mm 掌根球、2 mm 非指尖净距和自碰撞修复，并给内部净距目标增加 0.25 mm
  数值安全带，最终粗关键帧和插值验收仍严格使用 2.000 mm。
- v21 的 10 mm 单食指步态通过 37.3 mm 帧：食指法向误差 2.89 mm、掌根误差
  0.83 mm，碰撞硬审核通过；39.8 mm 帧仅剩食指进度误差 4.29 mm。
- v22 的 12 mm 多指步态在 37.3 mm 食指法向误差 3.02 mm；v23 的 10.5 mm
  单食指步态使掌部净距降到 1.481 mm。两者均已拒绝。
- v24/v25 的进度与法向高权重修复仍在 39.8 mm 留下约 3.05 mm 食指法向误差；
  v26 更高法向权重把违规转移到 1.739 mm 掌部净距，v27 掌根倾斜又把指腹角推到
  约 49.44°，均拒绝。
- v28 改用掌根目标自身的局部表面法向，37.3 mm 种子净距提高到约 2.64 mm，
  39.8 mm 仅剩中指进度 4.29 mm；证明局部法向优于四指平均法向。
- v30 增加独立的后段半正弦指尖修正窗口，37.3 mm 同时通过法向、掌根球和碰撞；
  39.8 mm 仅食指进度超差。v31 相同早段参数却在修正窗口生效前分叉，定位到
  环境 seed 未固定。
- v32/v32b 设置 seed 42 后仍分别在 37.3/34.8 mm 进入不同局部解；根因进一步
  定位为 GPU 接触校准得到的加载关节状态存在微小非确定漂移。规划器现支持
  `--planner-state-quantization-rad`：只量化非凸 MPC 初值，真实 MCC/接触反馈
  保持原始 GPU 状态；OpenBLAS/OMP/MKL 规划线程固定为 1。语法和 10/10 核心测试通过。
- v33/v33b 使用 0.0005 rad 相对实时命令量化，仍分别在 37.3/34.8 mm 分叉；
  因为接触搜索中的实时命令也依赖 GPU 加载状态，量化锚点现改为加载资产的固定抓取。
- 根据用户新要求，增加显式三支撑接触调度：规划中每帧至少 3 指保持 3 mm 名义
  接触带；一次仅一个指定手指可在限定窗口使用 6 mm 离面带；插值计划逐帧复查。
  运行时记录最少同时触觉接触数、每指最长失联帧数，并要求路线末段四指连续恢复。
- v34 在 27.4–37.3 mm 连续得到 3/4 规划接触，食指法向误差约 3.00–4.42 mm，
  其余三指保持 3 mm 内；39.8 mm 因掌部避障修复后只有 1/4 接触而拒绝。
- 掌根 3 mm 可行球不再只沿内法向取样；新增外法向、正负表面切向、正负经线和
  内法向+切向组合种子，目标是在物体侧面绕开掌部碰撞并保留三指支撑。
- v35 的扩展掌根球在 39.8 mm 避免了 v34 的多指离面；失败项仅为经线进度误差
  `[4.70, 4.14, 4.08, 0.02] mm`。新增重定位窗口专用 5 mm 进度阈值，窗口外
  仍为 4 mm、最终仍为 3 mm。
- v36 在 39.8 mm 只剩无名指法向 3.22 mm，但候选评分会让轻微接触违规解压过
  真正可行解。现为进度、三支撑法向、切向和单调性硬违规统一增加百万级淘汰分，
  与掌根球/碰撞排序一致；3 mm 阈值保持不变。
- v37 使用硬约束优先排序后在 39.8 mm 找到三支撑、进度和指腹方向可行解，但
  掌部净距为 1.372 mm，小于 2.000 mm，拒绝。
- v38 把非接触掌根可行球从 3 mm 放宽到 4 mm，39.8 mm 掌部净距反而降到
  0.696 mm；说明需要修正掌根旋转/后段外移，而不是继续放大位置球，已恢复 3 mm。
- v39 把曲面姿态运输从 70% 降到 65%。39.8 mm 时保持 3/4 接触，四指法向误差
  `[3.20, 1.60, 1.98, 0.22] mm`，全部硬约束通过；42.3 mm 最优候选掌部净距
  1.845 mm，距 2.000 mm 门槛尚差 0.155 mm，已拒绝。
- v40 把二次掌根外移从 5.3 mm 增加到 5.6 mm 后，42.3 mm 净距反而降到
  1.626 mm；额外平移损害四指耦合可达性，拒绝。
- v41 恢复 5.3 mm 二次外移并把姿态运输降到 62.5%，42.3 mm 通过且保持
  3/4 接触；首次失败移到 44.8 mm，掌部净距为 1.545 mm。
- v42 使用 60% 姿态运输，通过 44.8 mm 并保持 3/4 接触；47.3 mm 掌部净距
  1.893 mm，只比 2.000 mm 门槛少 0.107 mm。
- v43 使用 59% 姿态运输，通过 47.3 mm；该帧三支撑法向/切向误差均通过。
  首次失败移到 49.8 mm，掌部净距为 1.588 mm。
- v44 全程使用 57.5% 时在 39.8 mm 前进入另一局部分支，掌根球误差
  3.399 mm 超过 3.000 mm，拒绝。现改为 59% 前段 + 平滑晚段降增益。
- 已新增可记录到计划资产的平滑晚段姿态增益。v45 使用 59% 前段、45–55 mm
  平滑降到 55%，连续通过 59.7 mm；62.2 mm 因食指重定位窗口早于 70 mm
  步态归零点结束，进度误差 5.46 mm，拒绝。
- v46 把指定食指窗口延到 72 mm、窗口进度带设为 6 mm 后通过 62.2 mm 进度，
  但食指切向误差 3.02 mm 超过统一 3.00 mm；其余三指均小于 1.70 mm。
- v47 使用指定食指 3.5 mm 切向带，通过 62.2–67.2 mm；69.6 mm 食指法向
  6.12 mm 超过窗口 6.00 mm，其余三指仍接触。
- v48 把恢复窗口延到 75 mm、指定食指法向带设为 6.5 mm，通过 69.6 mm；
  72.1 mm 掌部净距 1.909 mm，比 2.000 mm 门槛少 0.091 mm。
- v49 只把晚段姿态增益从 55% 降到 54%，通过 72.1–74.6 mm；77.1 mm
  在平坦窗口截止后食指进度误差 4.97 mm，说明固定宽松带不能主动驱动渐进恢复。
- 新增 `--transient-contact-recovery-start-m`。指定食指在恢复起点到窗口终点
  之间使用 smoothstep，把法向、切向与进度带连续收紧到名义四指阈值；三根
  支撑手指始终使用名义阈值，窗口外强制四指名义接触。
- v50 从 65 mm 开始恢复，但食指主步态到 70 mm 才归零，两个目标发生冲突。
  72.1 mm 时三根支撑指法向误差为 0.15–1.45 mm，食指为 6.39 mm，高于
  恢复曲线已收紧到的 5.48 mm；全程最少 3 指接触规则本身仍保持。该运行拒绝。
- v51 把统一恢复起点移到 71.5 mm，已通过 72.1 和 74.6 mm；77.1 mm 时
  三根支撑指仍为 0.24–1.29 mm，但食指法向 6.19 mm 超过已收紧到的
  5.19 mm。统一收紧仍把沿面重定位与法向落指耦合过紧，运行拒绝。
- 新增可选的独立法向恢复起点，形成“抬起—沿面重定位—落下”分相调度：
  进度/切向先收紧，法向随后收紧，窗口终点仍强制四指恢复。
- v52 使用分相恢复后通过 77.1 mm：指定食指法向 6.32 mm，三根支撑指为
  0.14–1.50 mm。79.6 mm 的首个失败改为掌根可行球 3.797 mm 超过
  3.000 mm，说明接触恢复已推进而晚段掌姿仍需减少耦合运动。
- v53 把整个晚段姿态运输降到 53%，在 77.1 mm 提前进入另一局部分支，
  掌部净距 1.911 mm 低于 2.000 mm，证明不能破坏 v52 已验证的 54% 分支。
- 新增第三阶段末段姿态增益，只在指定的终端窗口继续减小掌根旋转，避免改变
  已验证的前段/晚段分支。
- v54 的 74–82 mm 末段调度仍开始过早，74.6 mm 掌部净距降到 1.893 mm，
  低于 2.000 mm；该运行拒绝。末段调度必须在 v52 已通过的 77.1 mm 附近
  才开始。
- v55 从 77.0 mm 起平滑调度，但 77.1 mm 的微小增益变化已使非凸求解分叉，
  掌根位置误差 3.736 mm 超过 3.000 mm。第三阶段必须完全避开该关键帧。
- v56 完全保留 77.1 mm 的 54% 分支并通过该帧，但 79.6 mm 掌根误差仍为
  3.843 mm，证明继续减少末段掌姿旋转不能解决掌根球瓶颈。当前给失败诊断增加
  世界坐标及局部法向/方位/经线三轴误差，先确定末段掌根目标应修正的方向。
- v57 恢复已验证的 54% 分支并测得 79.6 mm 掌根局部偏差
  `[法向 -1.630, 方位 -1.314, 经线 -3.168] mm`。新增最大范数 3 mm 的
  末段局部掌根输入点修正；它移动明确的五点输入目标，不放宽实际掌根相对目标的
  3 mm 可行球。
- v58 使用 `[-0.5,-0.5,-1.0] mm` 局部掌根目标修正，保持至少 3/4 指腹
  接触并复现通过 77.1 mm；79.6 mm 掌根误差为 3.880 mm，局部分量
  `[法向 -0.678, 方位 -0.133, 经线 -3.819] mm`。法向与方位改善，但经线
  比 v57 恶化 0.651 mm，证明求解器沿经线对目标改动存在非刚性、过度跟随响应；
  该运行拒绝且没有可交付视频。
- v59 只把经线项反向为 +0.5 mm，79.6 mm 掌根误差降到 3.446 mm，局部
  分量为 `[法向 -1.892, 方位 0.004, 经线 -2.881] mm`。方向有效但仍超过
  3 mm；v57/v58/v59 的二维局部拟合最优范数仍约 3.433 mm，因此必须拆开
  此前绑定的法向与方位修正来识别第三个响应方向。
- v60 使用 `[-1.5,0,+0.75] mm` 得到第三方向，79.6 mm 掌根误差降到
  3.036 mm，局部分量 `[法向 0.183, 方位 -0.531, 经线 -2.983] mm`，
  仅超门槛 0.036 mm。四次测量可拟合完整 3×3 局部响应；无约束零误差解范数
  7.369 mm，不满足修正向量最大 3 mm 的硬限制，故不能采用。
- v61 使用修正球内全局线性解的 90% 点，但相对 v60 的经线改动过大，使非凸
  求解切换分支；79.6 mm 掌根误差升到 5.011 mm，局部分量
  `[法向 -0.105, 方位 -1.044, 经线 -4.900] mm`。该运行拒绝，说明只能在
  v60 邻域作小步局部校正。
- v62 使用 `[-1.47,-0.10,+0.86] mm` 小步修正后通过 3 mm 掌根球，但
  79.6 mm 没有全机碰撞可行候选；最佳掌部净距仅 0.901 mm，低于 2.000 mm。
  当前掌根球多起点只有单独 +0.5R 外法向，没有外法向与经线/方位组合，不能
  同时绕开掌部和保留四指耦合 IK。
- v63 全程启用新增外向组合多起点后，在 74.6 mm 提前选择不同分支；食指法向
  误差 6.51 mm，超过该帧调度阈值 6.50 mm，运行按硬规则拒绝。新种子只需要
  解决 77.5 mm 后的末段瓶颈，不应干扰已验证早段。
- v64 只在末段启用外向组合种子，复现通过 77.1 mm，并消除 79.6 mm 的掌根球
  与碰撞首项失败；新首项为指定食指进度 5.13 mm，其余三指进度误差
  0.40/0.60/1.50 mm。现有 71.5–85 mm 进度恢复在该帧已收紧过多。
- v65 把进度/切向恢复推迟到 75.2 mm 后，79.6 mm 进度不再是首项；新分支
  掌根球误差 3.235 mm，局部分量 `[法向 -1.207, 方位 0.189, 经线 -2.996] mm`。
- v66 使用 `[-1.67,-0.07,+0.56] mm` 后，79.6 mm 掌根球与碰撞不再是
  首项；指定食指法向为 6.06 mm，超过当时恢复曲线 5.45 mm，另外三指仅
  0.19/1.00/0.17 mm。需要延后法向闭合但保持 85 mm 四指恢复终点。
- v67 延后法向恢复到 78.5 mm 后，79.6 mm 只剩掌部净距 1.782 mm，
  比 2.000 mm 门槛少 0.218 mm；其余硬约束通过。
- v68 将末段局部掌根法向目标从 -1.67 mm 向外移到 -1.40 mm；已在 RTX 4090 D
  上复现通过 77.1 mm，并持续保持至少 3/4 名义指腹接触。79.6 mm 的原始
  掌部净距提高到 1.82 mm，但非凸修复切换了分支：指定食指法向误差 6.62 mm，
  高于该帧分相恢复带 6.24 mm，其余三指仅 0.02–1.63 mm。该运行严格拒绝，
  调试视频不作为关键进展交付。
- v69 改用更小的 -1.55 mm 法向目标，复现通过 77.1 mm，并使 79.6 mm 的
  最优候选掌部净距达到 1.927 mm；仅比 2.000 mm 硬门槛少 0.073 mm，
  食指接触不再是首项失败。按 v67→v69 的局部净距响应，v70 只再向外移动
  0.06 mm 到 -1.49 mm，避免靠近 v68 的分支切换点。
- v70 的 -1.49 mm 单轴目标没有线性改善，79.6 mm 最优净距反而为 1.875 mm；
  -1.55 mm 附近是当前单轴局部峰值。v71 恢复 v69 的五点目标，不再移动目标，
  改为在原 3 mm 掌根球内增加 0.90R 外法向、0.75R 外法向±0.30R 单切向及
  0.70R 外法向±0.35R 双切向外壳种子；所有种子范数仍小于 1。
- v71 的新增外壳种子全部收敛回 v69 同一局部解，79.6 mm 最优净距仍为
  1.927 mm。端点求解已接近可行，但 2.5 mm 关键帧间的线性关节插值仍擦过
  掌部；v72 把整条 0.48 m 路线从 193 加密到 385 个 MPC 关键帧，
  每段约 1.25 mm，以缩短插值弦并重新求解中间位姿。

下一步：

1. 先把碰撞约束拆成 FR3 严格禁碰与 LEAP Hand 受控接触两套独立审核：
   FR3 保留 2 mm 规划净距且运行时任何接触判失败；手部非 fingertip pad 接触允许，
   但不计入指尖支撑，并增加最大接触力、最大穿透和“不得导致指尖支撑下降”的记录/
   终止条件。用测试确认掌部接近不再阻止可达指尖计划，FR3 仍会严格阻止。
2. 使用 seed 42、固定抓取锚点 0.0005 rad 量化、3 mm 扩展掌根球、至少 3 指接触、
   局部掌根法向、10 mm 单食指主窗口和 1 mm 后段修正，保持 5.3 mm 二次掌根
   外移；前段使用 59% 曲面姿态运输，从 45 mm 起在 10 mm 内平滑降到 54%。
   v77 使用 193 个关键帧，并恢复 v52 的 59%→54% 姿态分支，不启用第三阶段；
   使用 25–100 mm
   指定食指重定位窗口，进度/切向和法向均从 85 mm 起平滑恢复：法向 6.5→3 mm、
   切向 3.5→3 mm、进度 7.5→4 mm。从 77.5–80.0 mm 平滑建立局部掌根目标修正
   `[-1.55, -0.07, +0.56] mm`（法向/方位/经线），恢复 v69 当前最优目标；
   92.1–94.5 mm 再平滑增加 `[0, 0, +0.5] mm` 第二阶段测量扰动。该扰动把
   94.5 mm 掌根误差从 4.415 mm 降至 4.202 mm，但仍超过当时的 3 mm，故
   v77 拒绝。用户随后把掌根改为被动软方向引导；v79 恢复 +0.5 mm 小修正，
   启用 `--palm-guide-only --palm-guide-max-drift-mm 30`。3 mm 参数仅保留为
   局部多起点搜索半径，不再是掌根成败阈值；四指表面硬约束与全部碰撞/接触
   阈值不变。AST、CLI 和 12/12 MCC/碰撞策略回归测试通过；
   v79 已通过 94.5 mm 并在 97.0 mm 恢复 4/4，99.5 mm 只剩前两指进度
   4.28/4.23 mm 略超 4 mm。v80 的两级指尖进度优先修复没有改变该局部解；
   v81 保持阈值并把 193 帧加密为 385 帧，每段约 1.25 mm，在 99.7 mm 将
   最大指尖进度误差降到 4.05 mm，但仍有食指/拇指各超 0.05 mm；
   v82 已实现 90–110 mm 局部二倍加密，实际 402 帧，并保持固定 4 mm
   硬门槛；静态/回归检查已通过。GPU 结果在 98.5 mm 因中指 4.15 mm
   严格失败；v83 把加密起点推迟到 98.6 mm，保留此前可行支路，但新增
   99.1 mm 半步食指进度仍为 4.20 mm；v84 将单独规划进度同步终点到
   102 mm，同时保持 100 mm 后 4/4 物理接触；实现和静态/回归检查已完成，
   但 GPU 在 98.5 mm 因掌部穿入 1.041 mm 拒绝；v85 改为只重写首次失败
   区间内的食指目标点；
   继续使用原有外向组合，并增加 0.90R 外法向、0.75R 外法向±0.30R
   单切向和 0.70R 外法向±0.35R 双切向外壳种子；
   +0.4R 外法向±0.5R 方位/经线及
   +0.5R 外法向±0.35R 方位+0.35R 经线组合种子，全部仍在原 3 mm 球内，
   并且只在 77.5 mm 末段修正开始后启用。
   支撑三指全程及 85 mm 后
   四指都保持名义 3 mm 接触带，其余硬阈值不变。
3. 在可复现分支上根据首个失败帧调整独立后段指尖窗口；若圆柱段或上端帽面再次
   失败，继续用掌根可行球和局部异步轨迹联合求解，
   不放宽碰撞、法向或自碰撞阈值。
4. 动态自碰撞仍按 0.05 mm 严格验收；输出视频并抽帧审核指腹接触、拇指可见、顶部到达、
   FR3 无物体接触、其它手部接触受控且不破坏指尖支撑、无手指自碰撞；失败则继续改。
5. 补 FR3 回归测试、两级 README，执行多物体/大曲率泛化测试并更新 #7/#4。
6. 分阶段更新两级 PROCESS，提交并直接推送 `main`。

## 2026-07-28 检查点：v97 结果与 v98 自适应细分

- v97 保持掌根被动软方向引导和四指腹严格规划，成功越过 v96 的
  124.6753 mm 瓶颈并推进到 155.8 mm；已接受关键帧保持 4/4 指腹，
  FR3 净距约 95–98 mm，手部附带穿入不超过约 0.75 mm。
- 首个硬失败为 157.1 mm 的四指纵向进度误差
  `[4.10,3.49,2.02,3.98] mm`，仅食指超过固定 4.00 mm 门槛 0.10 mm；
  不是 FR3 碰撞、掌根漂移或接触丢失。v97 视频只归入 debug。
- v98 已实现 failure-triggered midpoint shooting refinement：普通优化、
  修复和 bounded rephase 后仍违反任一进度/法向/切向/单调性/掌根防漂移/
  FR3-手部碰撞/自碰撞/指腹朝向硬约束时，自动在该 shooting 区间插入中点，
  以更短沿面步长重新求 URDF 可达状态；最小步长与最大插入数防止无限细分，
  插入距离和原因写入 NPZ。没有放宽任何验收门槛。
- v98 的 AST、CLI 新参数和 12/12 MCC/碰撞策略回归测试已通过。下一步用
  v97 同一严格配置增加自动细分参数运行 GPU；若完整 0.48 m 规划通过，
  再执行 4700 步动力学和逐帧视频审核，未审核视频不得进入 deliverables。
- 同步记录于 GitHub issue #7 评论 `5108288620`。

### v98 动态审核失败与 v99 修复

- v98 在 157.1 mm 打印自动插入后直接进入下一索引，没有求解新增中点；
  源码审核确认 adaptive planner 仍是静态 `for keyframe`，插入数组行后的
  `continue` 会跳过该行；一次文本替换还误改了不相关的环向规划循环。
  因此 v98 约 199.5 mm 前的插入结果无效，运行已终止，视频只可作为 debug。
- v99 恢复环向规划原 `for`，并将 adaptive surface MPC 改为真正依赖动态
  `keyframe_count` 的 `while`；只有关键帧通过全部硬约束后才递增索引，
  失败插入中点后保持当前索引并重新求解新增行。
- 新增 AST 结构回归：adaptive planner 禁止 `for keyframe`，必须且只能有
  一个动态 keyframe `while`，并检查接受后的索引递增。AST 和 13/13
  回归测试通过。issue #7 已同步评论 `5108529688`；下一步重跑 v99。

### v99 严格 GPU 结果与 v100 局部相位延续

- v99 证明动态索引修复有效：157.091 mm 首次插入 156.468 mm 中点后，
  日志在同一 keyframe 真正求解并接受该中点，再接受原 157.091 mm 目标；
  因此 v98 的静态索引错误已经排除。
- 规划连续推进至 229.403 mm。已打印关键帧保持 4/4 物理指腹，FR3 最近
  净距约 95–98 mm，掌根软引导误差约 6–8 mm，LEAP 附带接触穿入未超过
  1 mm。
- 在 210.701–229.558 mm 的若干局部支路，v99 共执行 10 次真实中点细分。
  首个无法继续的点为 229.558 mm；左侧区间已经细化到 0.156 mm，四指纵向
  进度误差为 `[3.73,0.85,1.94,4.12] mm`，只有拇指超过固定 4.00 mm
  门槛 0.12 mm。该处不是 FR3 碰撞、掌根漂移、指腹离面或手部过深穿透。
- 当前拇指自动相位已达到 -0.8 mm 配置边界，继续把 shooting 区间从
  0.156 mm 二分到阈值以下不会改变该饱和边界。因此 v99 严格拒绝，视频
  只保留在 `outputs/debug/20_fr3_planning/`。
- v100 保持全部指尖、接触、FR3、手部穿透、自碰撞和指腹角硬阈值不变，
  只把 failure-triggered per-finger rephase 最大连续相位从 0.8 mm
  提高到 1.2 mm。它仍是相对 0.48 m 路线很小的 URDF shooting 相位，
  并在最终 20 mm 内强制平滑回零；目标是从已饱和的 -0.8 mm 拇指支路
  以 0.05 mm 小步继续，不放宽 4 mm 指尖进度验收。

### v100 严格 GPU 结果与 v101 前驱可行桥

- v100 把最大 rephase 边界提高到 1.2 mm 后，真实越过 v99 的
  229.558 mm 饱和点，并连续推进到 287.844 mm。已接受关键帧保持
  4/4 物理指腹；FR3 最近净距约 92–97 mm；掌根被动软引导误差约
  6–10 mm；LEAP 允许的附带穿入保持小于 1 mm。
- 287.377、287.688 和 287.844 mm 分别由动态中点细分插入并通过。
  287.844 mm 接受的四指相位偏移为
  `[-0.40,-0.363,-0.40,-1.14] mm`，四指纵向进度误差为
  `[2.51,2.28,1.98,3.96] mm`，全部仍在固定 4.00 mm 硬门槛内。
- 仅相隔 0.156 mm 的原 288.000 mm 目标却收敛到另一局部分支，四指误差
  变为 `[5.50,2.61,2.22,3.82] mm`，食指突然超差；这不是接触丢失、
  FR3/手部碰撞或掌根漂移。现有 rephase 优化只把上一已接受 `previous_q`
  当 least-squares 种子，没有把它作为独立硬可行候选，因此优化器可在
  极短区间内离开连续前驱分支。v100 严格拒绝，视频只归 debug。
- v101 将实现 failure-triggered predecessor feasibility bridge：先在上一
  已接受 URDF 状态上计算每根指尖当前目标的有符号进度，只把超出内侧安全带
  的最小量投影为 per-finger 目标相位；然后直接审核该前驱状态的进度、
  法向/切向、支撑数、单调性、掌根防漂移、FR3 2 mm 净距、LEAP 1 mm
  穿入、自碰撞和 50° 指腹角。只有全部原硬门槛通过才接受桥接，并写入
  NPZ；否则仍进入原优化/修复/重相位/中点细分链。
- v101 计划把相位搜索边界设为 1.5 mm，主要覆盖已经达到 -1.14 mm 的
  拇指连续支路；最终 20 mm 强制回零和共同终点不变，所有物理验收门槛
  不放宽。

#### v101 实现检查点

- `demo_surface_slide.py` 已实现 predecessor feasibility bridge。失败触发后
  先用 `previous_q` 计算有符号四指进度，把超过硬门槛内侧 0.05 mm
  安全带的最小量投影到目标相位；不改变关节状态。
- 桥接候选会独立重跑进度、法向/支撑、切向、单调性、掌根防漂移、
  FR3/LEAP/自碰撞/指腹角和关节限位检查。只有全部通过才以
  `[FEASIBILITY-BRIDGE]` 接受；否则继续原 bounded rephase。
- 新增 `mpc_coarse_feasibility_bridge` 布尔 NPZ 字段；动态中点插入时
  同步插入该数组，最终仅在整帧硬验收通过后写入桥接标记，防止错位。
- 新增源码结构回归，确认桥接直接复制 `previous_q` 且包含全部硬检查与
  NPZ 记录。AST 和 14/14 MCC/接触/结构测试通过；wandb 临时目录清理仍有
  既有 Windows 权限警告，但测试进程返回 0。
- 首次现场运行发现两个执行层问题并主动终止：压缩恢复命令最初漏掉 v100
  的无碰撞接近方向，默认 +X 退回使物体在规划前碰到 FR3 link7；CPU 对完整
  接近线扫描后恢复 `--object-retreat-azimuth-deg -90`，该路径 FR3 最小
  净距为最终抓取处的 28.702 mm，退回端为 207.163 mm。
- 随后的动态日志在 1.247 mm 普通粗区间提前接受桥接并连续原地保持，虽逐帧
  仍在硬带内，却会制造不可接受的停顿。因此运行再次主动终止。桥接现只允许在
  区间不大于 `max(0.25 mm, 2*auto_refine_min_step)` 时触发；当前配置即
  0.30 mm，只覆盖无法再二分的极短区间（目标 0.156 mm），不会替代正常 MPC
  移动。AST 与 14/14 回归再次通过。
- 第二次运行确认短区间门控有效，但全程把普通 rephase 上限从 1.2 提到
  1.5 mm 会在 94.8 mm 选择四指 `+1.5 mm` 饱和分支，并在 97.9 mm
  提前动态细分，偏离 v100 已验证早段，故再次主动终止。现新增
  `--mpc-feasibility-bridge-max-mm`：普通非凸 rephase 继续严格使用 v100
  的 1.2 mm，只有 ≤0.30 mm 的前驱桥可用 1.5 mm；两个上限均写入 NPZ。
  这样末端修复不会改变 0–287.844 mm 的普通候选集合。AST 与 14/14
  回归再次通过。

#### v101 严格 GPU 失败：连续静态桥累计停顿

- 第三次 v101 保持普通 rephase 1.2 mm、短桥 1.5 mm、正确的
  `--object-retreat-azimuth-deg -90` 和全部原硬阈值。0–173.299 mm
  复现 v100 早段；94–100 mm 没有再出现全局 1.5 mm 分支污染。已接受帧
  保持至少 3 指、恢复后 4/4 指腹；FR3 净距约 27–47 mm，LEAP 非指尖
  规划穿入小于 1 mm。
- 173.299→173.455 mm 细分到 0.156 mm 后，静态前驱桥把第四指相位投影到
  -1.369 mm；下一段又以 -1.500 mm 复用同一 `previous_q`。两次单帧都
  通过硬约束，但累计形成 0.312 mm 零关节运动。到 173.922 mm，第四指
  进度误差升到 4.23 mm，超过固定 4.00 mm 门槛，运行严格终止。
- 根因是短区间门控只保证单帧静态前驱可行，不能保证连续桥接后的轨迹连续。
  下一版改为 moving trust-region bridge：1.5 mm 短步相位范围优先用于
  `previous_q` 邻域的局部求解，要求产生实际关节/指尖运动并重审全部硬
  约束；零运动前驱仅允许单次最后兜底，禁止连续使用。
- 用户新增资源规则：先用 headless 模式完成整条 0.48 m 规划和动力学数值
  验收；失败只保留日志/NPZ，不再编码大量 debug MP4。只有物理硬指标全部
  通过后才复跑录像并做视觉审核。该规则已同步 issue #7 评论
  `5114578543`。
- moving bridge 与 headless 流水线已经实现：`--viewer headless` 只推进
  仿真和执行完整数值终审，不创建渲染上下文、不调用 `env.render()`、不打开
  imageio writer；moving bridge 使用 0.05 rad/关节局部信赖域，至少三指
  必须实际前进短区间的 10%，且需要相位修复的手指也必须前进。若 moving
  候选失败，静态桥只能使用一次，下一帧禁止再次静态保持。NPZ 分开记录全部
  bridge 和 static bridge。AST 与完整 15/15 回归通过；失败 v101 MP4
  已删除。

#### v102 headless 结果：禁止连续 static 生效，moving 仍缺少前进目标

- v102 没有生成 MP4，只保存 headless 日志。0–173.299 mm 复现既有分支；
  已接受帧按计划保持至少 3 指、恢复后 4/4，FR3 净距约 27–47 mm，LEAP
  非指尖规划穿入小于 1 mm。
- 173.299 mm 后细分至 0.156 mm。第一个 moving bridge 未找到满足“实际
  前进 + 全硬约束”的候选，因此按设计只用一次 static bridge；下一帧连续
  static 被正确禁止。173.766 mm 第四指进度误差 4.24 mm，超过 4 mm，
  headless 运行退出码 1。
- 根因是通用 MPC 残差把进度看作硬可行带，进入带内后没有独立的短步前进
  目标；局部求解仍可能停在 `previous_q`。v103 将给 moving bridge 增加
  专用沿面运动目标：以当前四指实际 arc 为起点，显式要求沿路线推进当前
  短区间长度；继续用原全部硬约束做最终接受，且打印 moving 拒绝分解。

#### 2026-07-29 聚合接触验收与有界停顿已实现

- 用户取消“逐帧至少三指、逐规划步必须移动”的过严要求。新规则已写入本文档
  头部，并同步 issue #7 评论 `5115007191`。
- 运行时允许短暂低于三指以及最长 10 帧的零指接触；连续低于多数接触阈值
  仍受 `--contact-failure-window` 约束，单指失联仍受各自恢复窗口约束。
- 终审不再用历史最少同时接触数一票否决，改为：至少 80% 的运动帧达到
  三指多数接触、平均同时接触数至少 3.0、每指接触率至少 75%，并在末端
  连续 20 帧恢复 4/4。瞬时最少接触数及最大连续失联仍完整记录。
- moving bridge 增加独立沿面目标，显式要求从当前实际四指 arc 推进当前
  短区间；信赖域、实际关节/指尖运动检查和全部碰撞硬约束不变。
- static bridge 由“禁止连续”改为有界兜底：连续覆盖路线最多 0.50 mm，
  全程静态桥总量最多 2%，临时进度带最多 4.50 mm；恢复移动后普通 4 mm
  门槛和末段共同终点仍不变。NPZ 记录逐帧 dwell、总静态距离和全部参数。
- AST、差异检查和完整 16/16 回归通过；wandb 的既有 Windows 临时目录
  清理警告仍存在但测试退出码为 0。下一步只跑 v103 headless 数值验收，
  物理全程通过前不生成新视频。

#### v103 headless 结果与 v104 调整

- v103 未生成视频。规划越过旧版 173.3 mm 失败点，并连续推进到
  268.675 mm；全部已接受关键帧保持 `4/4` 指腹规划接触、FR3 零物体
  接触、自碰撞为零，LEAP 附带压入小于 1 mm。
- 在 268.208–268.675 mm 局部可行域内，规划器需要短暂保持上一 URDF
  可达姿态。累计停顿约 0.467 mm；下一段 0.156 mm 仍需保持时触发原
  `0.50 mm` 单段上限，随后以纵向进度误差
  `[3.02,4.56,3.91,4.48] mm` 严格退出。失败不是碰撞或动力学接触，
  而是短暂停顿预算过严。
- v104 只把单段静止桥默认上限从 `0.50 mm` 放宽到 `1.00 mm`。全程静止
  桥累计仍不得超过 0.48 m 路线的 `2%`，普通移动帧、最终恢复、
  FR3/LEAP/自碰撞和聚合接触终审均不放宽。先重新运行 headless；数值
  全通过前仍不生成视频。

#### v104 headless 结果与 v105 有界恢复窗口

- v104 未生成视频或成功计划。它在 268.208–268.987 mm 之间保持上一
  URDF 可达姿态，累计静态覆盖约 0.779 mm；已接受帧仍为 `4/4` 指腹，
  FR3 净空约 81.5 mm、自碰撞为零、LEAP 附带压入小于 1 mm。
- 规划继续细分到 269.143 mm 时，累计停顿约 0.935 mm，纵向进度误差为
  `[2.71,4.88,4.14,4.79] mm`。该帧同时受 1.00 mm 单段停顿预算和
  4.50 mm 临时进度带约束，最终严格退出；失败仍不是物理接触或碰撞。
- v105 将单次静态恢复覆盖上限改为 `1.50 mm`，并只在该有界恢复窗口内
  使用 `6.00 mm` 临时纵向容差。全程静态总量仍最多为 0.48 m 路线的
  `2%`；普通移动帧继续使用 4 mm、末端必须恢复共同终点和稳定四指接触。
  FR3 零物体接触、LEAP 力/压入、自碰撞、指腹朝向和聚合接触终审均不变。
- issue #7 已同步评论 `5116773444`。下一步先完成静态/回归测试，再运行
  v105 headless；数值全通过前仍不生成视频。

#### v105 headless 失败与 v106 有界移动恢复

- v105 未生成视频或成功 NPZ。它通过了 173、188、191–192、233 mm 的
  既有局部瓶颈；25–100 mm 早期重定位短暂使用 `3/4` 后约 97 mm 恢复
  `4/4`，之后已接受关键帧均保持四指规划接触。FR3 在 269 mm 附近仍有
  约 81.6 mm 净空、自碰撞为零、LEAP 附带压入小于 1 mm。
- 268.208–269.610 mm 连续静态恢复累计约 1.402 mm；下一目标
  269.766 mm 必须移动时，四指纵向误差为
  `[2.08,5.50,4.76,5.42] mm`，仍无法回到普通 4 mm 进度带。
  因此继续延长静态等待不是合理修复：它不会改变 URDF 可达分支，只会让
  轨迹靠停顿通过。
- v106 已实现“有界移动恢复 + 整段统计验收”。静态和放宽的移动恢复共享
  最长 `3.00 mm` 连续覆盖预算与全路线 `3%` 总预算；其中 static 自身仍
  最长 `1.50 mm`、全程最多 `2%`。移动恢复临时允许 `6.00 mm` 纵向误差、
  最少两指处于名义 3 mm 接触带且四指均不得离表面超过 `6.50 mm`，但仍
  必须有非零关节运动、至少三指真实前进，并继续满足切向、单调、掌根防漂移、
  FR3 零接触、LEAP 穿入、自碰撞、指腹角和关节限位硬约束。
- 恢复区间逐关键帧和逐动力学插值帧写入 NPZ；恢复禁止进入最后 `20 mm`。
  计划插值后新增整段审核：多数接触帧占比、平均同时接触数、每指接触率及
  末端连续四指恢复均必须达到与运行时相同的阈值。新增结构回归后完整
  `17/17` 测试和 CLI 检查通过；测试退出码为 0，既有 wandb Windows 临时
  目录清理警告不影响测试。
- issue #7 已同步评论 `5117379121`。下一步提交 v106 检查点并只运行
  headless GPU 数值验收；完整 0.48 m 规划与 4700 步动力学通过前不生成
  视频。
